# ============================================
# DOCKER COMPOSE - Proyecto API de Centro de call center
# ============================================

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1